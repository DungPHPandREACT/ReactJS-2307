import React from 'react';
import styles from './card-product.module.css';

const CardProduct = () => {
	// viết js

	return <h1 className={`${styles.heading} ${styles.fontSize}`}>Card product</h1>;
};

export default CardProduct;
